#include <stdio.h>
#include <ctype.h>

int any2d(int base);
void main()
{
  int valeur;
  char c;
  c = getchar();
  while(c!= '\n') {
    if(base == 2 && valeur != -1) {
      // verification base2 a faire
      // conversion decimale a faire
    }
    else if(base == 4 && valeur != -1) {
      // verification base4 a faire
      // conversion decimale a faire
    }
    else if(base == 10 && valeur != -1) {
      // verification base10 a faire
      // conversion decimale a faire
    }
    else if(base == 16 && valeur != -1) {
      // verification base16 a faire
      // conversion decimale a faire
    }
    else {
      valeur = -1;
    }
  }
  printf("%d\n",valeur);
}
